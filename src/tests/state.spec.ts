import {
  addTodo,
  getCurrentState,
  deleteItem,
  markComplete,
  markIncomplete,
  resetState,
  undoState,
  state,
} from "../state";

beforeEach(() => {
  // reset
  resetState();
});

test("add todo should add a todo item to the state", () => {
  // todo: Write test
});
